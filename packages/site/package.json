{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf src/generated",
    "prebuild": "npm run generate:content",
    "generate:content": "tsx generateContent.ts",
    "dev": "patternfly-doc-core start",
    "build": "patternfly-doc-core build",
    "serve": "patternfly-doc-core serve",
    "sync": "patternfly-doc-core sync",
    "init:docs": "patternfly-doc-core init"
  },
  "dependencies": {
    "astro": "^5.7.13"
  },
  "devDependencies": {
    "@patternfly/patternfly-doc-core": "^1.8.3",
    "tsx": "^4.19.0"
  }
}
